import * as getmodel from "./getmodel";

export var getTestModel  = getmodel.getTestModel;
import * as constants from "./constants";

export const MONGOOSE_RECORD_REPLAY_FOLDER = constants.MONGOOSE_RECORD_REPLAY_FOLDER
export const ENV_NAME_MONGO_RECORD_REPLAY = constants.ENV_NAME_MONGO_RECORD_REPLAY;
